name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - scikit-learn
  - tqdm
  - matplotlib
  - networkx
  - pip:
    - pylcs
    - ray
    - timedinput
    - argparse-tui

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

