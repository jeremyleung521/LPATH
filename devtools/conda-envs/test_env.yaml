name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python<3.12
  - pip
  - scikit-learn
  - tqdm
  - matplotlib
  - networkx
  - pip:
    - pylcs
    - ray
    - timedinput
    - argparse-tui

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

