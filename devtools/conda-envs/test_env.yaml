name: test
channels:

  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - westpa>=2022.03
  - scikit-learn
  - pip
  - pip:
    - pylcs
    - ray


    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

