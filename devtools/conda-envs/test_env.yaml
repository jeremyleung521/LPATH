name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python<3.11
  - pip
  - scikit-learn
  - tqdm
  - matplotlib
  - networkx
  - pip:
    - pylcs
    - ray
    - timedinput

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

