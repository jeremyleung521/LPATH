name: test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - scikit-learn
  - tqdm
  - matplotlib
  - pip:
    - pylcs
    - ray
    - timedinput
    - argparse-tui
    - networkx

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

